'use strict';

describe('myApp.home module', function () {

  beforeEach(module('myApp.home'));

  describe('homee controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var homeeCtrl = $controller('HomeCtrl');
      expect(homeeCtrl).toBeDefined();
    }));

  });
});